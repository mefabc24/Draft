<UserControl
    x:Class="Draft.Views.Settings.AboutSettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:section="clr-namespace:Draft.Controls.Settings"
    xmlns:settings="clr-namespace:Draft.Wpf.Controls.Settings"
    d:DesignHeight="500"
    d:DesignWidth="540"
    mc:Ignorable="d">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Styles/Settings/SettingsTheme.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Border Background="{StaticResource Brush.Background.Window}">
        <ScrollViewer
            HorizontalScrollBarVisibility="Disabled"
            Style="{StaticResource SettingsScrollViewer}"
            VerticalScrollBarVisibility="Auto">
            <StackPanel VerticalAlignment="Top">
                <TextBlock
                    MaxWidth="420"
                    Margin="0,8,0,32"
                    HorizontalAlignment="Left"
                    FontFamily="{StaticResource Font.Inter}"
                    FontSize="14"
                    Foreground="{StaticResource Brush.Text.Secondary}"
                    Text="{Binding Description}"
                    TextWrapping="Wrap" />

                <section:SectionHeader Title="Draft" />

                <settings:SettingFieldCard Title="About Draft">
                    <TextBlock
                        FontFamily="{StaticResource Font.Inter}"
                        FontSize="14"
                        Foreground="{StaticResource Brush.Text.Primary}"
                        Text="Draft is a focused Markdown editor with live preview. This sample page is ready for future product details."
                        TextWrapping="Wrap" />
                </settings:SettingFieldCard>
            </StackPanel>
        </ScrollViewer>
    </Border>
</UserControl>
