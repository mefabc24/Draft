<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:Draft.Shell.Controls">
    <Style x:Key="WindowControlButton" TargetType="Button">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Height" Value="40" />
        <Setter Property="Width" Value="60" />
        <Setter Property="Foreground" Value="{StaticResource Brush.WindowControlIcon}" />
        <Setter Property="Tag" Value="Default" />
        <Setter Property="controls:ControlButtonAssist.CornerRadius" Value="0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border
                        x:Name="border"
                        Background="{TemplateBinding Background}"
                        CornerRadius="{Binding Path=(controls:ControlButtonAssist.CornerRadius), RelativeSource={RelativeSource TemplatedParent}}">
                        <ContentPresenter />
                    </Border>

                    <ControlTemplate.Triggers>
                        <!-- Close Hover -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="True" />
                                <Condition Property="Tag" Value="Close" />
                            </MultiTrigger.Conditions>
                            <Setter Property="Background" Value="{StaticResource Brush.CloseButtonBackground}" />
                            <Setter Property="Foreground" Value="{StaticResource Brush.Icon.WindowControlClose}" />
                        </MultiTrigger>

                        <!-- Default Hover -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="True" />
                                <Condition Property="Tag" Value="Default" />
                            </MultiTrigger.Conditions>
                            <Setter Property="Background" Value="{StaticResource Brush.ButtonHover}" />
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
