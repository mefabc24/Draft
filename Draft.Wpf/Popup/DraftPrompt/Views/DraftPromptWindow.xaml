<Window
    x:Class="Draft.Popup.DraftPrompt.Views.DraftPromptWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="Draft Prompt"
    Width="520"
    AllowsTransparency="True"
    Background="Transparent"
    Icon="/Assets/Icons/draft.ico"
    ResizeMode="NoResize"
    ShowInTaskbar="False"
    SizeToContent="Height"
    WindowStartupLocation="CenterOwner"
    WindowStyle="None"
    mc:Ignorable="d">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Styles/Settings/SettingsTheme.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <WindowChrome.WindowChrome>
        <WindowChrome
            CaptionHeight="64"
            GlassFrameThickness="0"
            ResizeBorderThickness="6" />
    </WindowChrome.WindowChrome>

    <Border Padding="24" Background="Transparent">
        <Border Style="{StaticResource DraftPromptSurface}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="auto" />
                </Grid.RowDefinitions>

                <Border Padding="16">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <TextBlock
                                VerticalAlignment="Center"
                                FontFamily="{StaticResource Font.Manrope}"
                                FontSize="20"
                                FontWeight="Bold"
                                Foreground="{StaticResource Brush.Text.Primary}"
                                Text="{Binding Title, RelativeSource={RelativeSource AncestorType=Window}}" />

                            <!-- Close Button -->
                            <Button
                                Grid.Column="1"
                                VerticalAlignment="Top"
                                Click="CloseButton_Click"
                                Style="{StaticResource DraftCloseButton}"
                                WindowChrome.IsHitTestVisibleInChrome="True">
                                <Path
                                    Width="12"
                                    Height="12"
                                    Data="{StaticResource Icon.Close}"
                                    Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                                    Stretch="Uniform" />
                            </Button>
                        </Grid>

                        <ContentControl
                            Grid.Row="1"
                            Margin="0,20,0,0"
                            Content="{Binding PromptContent, RelativeSource={RelativeSource AncestorType=Window}}">
                            <ContentControl.Style>
                                <Style TargetType="ContentControl">
                                    <Style.Triggers>
                                        <Trigger Property="Content" Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </ContentControl.Style>
                        </ContentControl>
                    </Grid>
                </Border>


                <Border
                    x:Name="PromptActionsContainer"
                    Grid.Row="1"
                    BorderThickness="0,1,0,0"
                    BorderBrush="#1B1B1B"
                    Padding="16"
                    Background="#0D0D0D"
                    CornerRadius="0,0,8,8"
                    Visibility="Collapsed"
                    HorizontalAlignment="Stretch">
                    <StackPanel
                        x:Name="PromptActionsPanel"
                        Height="30"
                        HorizontalAlignment="Right"
                        Orientation="Horizontal" />
                </Border>
            </Grid>
        </Border>
    </Border>
</Window>
