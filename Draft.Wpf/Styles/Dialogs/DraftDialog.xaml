<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="clr-namespace:Draft.Dialogs.Views"
    xmlns:models="clr-namespace:Draft.Dialogs.Models">

    <Geometry x:Key="DraftDialog.Icon.Info">M 12,2 A 10,10 0 1 1 11.99,2 M 12,10 L 12,17 M 12,7 L 12,7.1</Geometry>
    <Geometry x:Key="DraftDialog.Icon.Warning">M 12,3.2 C 12.4,3.2 12.8,3.4 13,3.8 L 21.5,18.5 C 21.9,19.2 21.4,20 20.6,20 L 3.4,20 C 2.6,20 2.1,19.2 2.5,18.5 L 11,3.8 C 11.2,3.4 11.6,3.2 12,3.2 Z M 12,9 L 12,14 M 12,17 L 12,17.1</Geometry>
    <Geometry x:Key="DraftDialog.Icon.Error">M 12,2 A 10,10 0 1 1 11.99,2 M 12,7 L 12,14 M 12,17 L 12,17.1</Geometry>
    <Geometry x:Key="DraftDialog.Icon.Success">M 12,2 A 10,10 0 1 1 11.99,2 M 7,12 L 10.5,15.5 L 17,8.5</Geometry>

    <converters:DraftDialogIconGeometryConverter x:Key="DraftDialogIconGeometryConverter" />
    <converters:DraftDialogBrushConverter x:Key="DraftDialogBrushConverter" />

    <SolidColorBrush x:Key="DraftDialog.Brush.TextOnInfo" Color="#0B2A54" />
    <SolidColorBrush x:Key="DraftDialog.Brush.TextOnWarning" Color="#3A2708" />
    <SolidColorBrush x:Key="DraftDialog.Brush.TextOnError" Color="#4A1014" />
    <SolidColorBrush x:Key="DraftDialog.Brush.TextOnSuccess" Color="#0E301A" />

    <Style x:Key="DraftDialogSurface" TargetType="Border">
        <Setter Property="Padding" Value="20,20,20,20" />
        <Setter Property="Background" Value="{StaticResource Brush.Background.Surface}" />
        <Setter Property="BorderBrush" Value="#3D3D3D" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="8" />
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect
                    BlurRadius="24"
                    Direction="270"
                    Opacity="0.28"
                    ShadowDepth="6"
                    Color="{StaticResource Color.Shadow.Default}" />
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="DraftDialogTitleText" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{StaticResource Font.Manrope}" />
        <Setter Property="FontSize" Value="24" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="Foreground" Value="{StaticResource Brush.Text.Primary}" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="TextAlignment" Value="Center" />
        <Setter Property="TextWrapping" Value="Wrap" />
    </Style>

    <Style x:Key="DraftDialogDescriptionText" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{StaticResource Font.Manrope}" />
        <Setter Property="FontSize" Value="16" />
        <Setter Property="FontWeight" Value="Regular" />
        <Setter Property="Foreground" Value="{StaticResource Brush.Text.Secondary}" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="LineHeight" Value="24" />
        <Setter Property="TextAlignment" Value="Center" />
        <Setter Property="TextWrapping" Value="Wrap" />
    </Style>

    <Style x:Key="DraftDialogActionButton" TargetType="Button">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Border.Default}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Foreground" Value="{StaticResource Brush.Text.Primary}" />
        <Setter Property="FontFamily" Value="{StaticResource Font.Manrope}" />
        <Setter Property="FontSize" Value="15" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Padding" Value="18,0" />
        <Setter Property="Tag" Value="{Binding DataContext.DialogType, RelativeSource={RelativeSource AncestorType=ItemsControl}}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border
                        x:Name="ButtonBorder"
                        Padding="{TemplateBinding Padding}"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="4">
                        <ContentPresenter
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            RecognizesAccessKey="True" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Cursor" Value="Hand" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.76" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.45" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <DataTrigger Binding="{Binding ActionType}" Value="{x:Static models:DraftDialogActionType.Secondary}">
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="BorderBrush" Value="{StaticResource Brush.Border.Default}" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="Foreground" Value="{StaticResource Brush.Text.Primary}" />
            </DataTrigger>
            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding ActionType}" Value="{x:Static models:DraftDialogActionType.Secondary}" />
                    <Condition Binding="{Binding IsMouseOver, RelativeSource={RelativeSource Self}}" Value="True" />
                </MultiDataTrigger.Conditions>
                <Setter Property="BorderBrush" Value="{StaticResource Brush.Background.Input}" />
            </MultiDataTrigger>
            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding ActionType}" Value="{x:Static models:DraftDialogActionType.Primary}" />
                    <Condition Binding="{Binding IsMouseOver, RelativeSource={RelativeSource Self}}" Value="True" />
                </MultiDataTrigger.Conditions>
                <Setter Property="Opacity" Value="0.88" />
            </MultiDataTrigger>
            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding ActionType}" Value="{x:Static models:DraftDialogActionType.Primary}" />
                    <Condition Binding="{Binding Tag, RelativeSource={RelativeSource Self}}" Value="{x:Static models:DraftDialogType.Info}" />
                </MultiDataTrigger.Conditions>
                <Setter Property="Background" Value="{StaticResource Brush.Function.Info}" />
                <Setter Property="BorderBrush" Value="{StaticResource Brush.Function.Info}" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="Foreground" Value="{StaticResource DraftDialog.Brush.TextOnInfo}" />
            </MultiDataTrigger>
            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding ActionType}" Value="{x:Static models:DraftDialogActionType.Primary}" />
                    <Condition Binding="{Binding Tag, RelativeSource={RelativeSource Self}}" Value="{x:Static models:DraftDialogType.Warning}" />
                </MultiDataTrigger.Conditions>
                <Setter Property="Background" Value="{StaticResource Brush.Function.Warning}" />
                <Setter Property="BorderBrush" Value="{StaticResource Brush.Function.Warning}" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="Foreground" Value="{StaticResource DraftDialog.Brush.TextOnWarning}" />
            </MultiDataTrigger>
            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding ActionType}" Value="{x:Static models:DraftDialogActionType.Primary}" />
                    <Condition Binding="{Binding Tag, RelativeSource={RelativeSource Self}}" Value="{x:Static models:DraftDialogType.Error}" />
                </MultiDataTrigger.Conditions>
                <Setter Property="Background" Value="{StaticResource Brush.Function.Critical}" />
                <Setter Property="BorderBrush" Value="{StaticResource Brush.Function.Critical}" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="Foreground" Value="{StaticResource DraftDialog.Brush.TextOnError}" />
            </MultiDataTrigger>
            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding ActionType}" Value="{x:Static models:DraftDialogActionType.Primary}" />
                    <Condition Binding="{Binding Tag, RelativeSource={RelativeSource Self}}" Value="{x:Static models:DraftDialogType.Success}" />
                </MultiDataTrigger.Conditions>
                <Setter Property="Background" Value="{StaticResource Brush.Function.Success}" />
                <Setter Property="BorderBrush" Value="{StaticResource Brush.Function.Success}" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="Foreground" Value="{StaticResource DraftDialog.Brush.TextOnSuccess}" />
            </MultiDataTrigger>
        </Style.Triggers>
    </Style>
</ResourceDictionary>
