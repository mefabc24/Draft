<UserControl
    x:Class="Draft.Settings.Views.Pages.PreviewSettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:section="clr-namespace:Draft.Settings.Controls"
    xmlns:settings="clr-namespace:Draft.Settings.Controls"
    d:DesignHeight="2000"
    d:DesignWidth="540"
    mc:Ignorable="d">
    <Border Background="Transparent">
        <ScrollViewer
            HorizontalScrollBarVisibility="Disabled"
            Style="{StaticResource SettingsScrollViewer}"
            VerticalScrollBarVisibility="Auto">
            <StackPanel VerticalAlignment="Top">
                <section:SectionHeader Title="RENDERING" />

                <settings:SettingFieldCard Title="Markdown theme" Margin="0,0,0,32">
                    <ComboBox
                        Height="36"
                        HorizontalAlignment="Stretch"
                        ItemsSource="{Binding MarkdownThemeOptions}"
                        SelectedItem="{Binding Settings.MarkdownTheme, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                        Style="{StaticResource SettingsComboBox}" />
                </settings:SettingFieldCard>

                <section:SectionHeader Title="LINKS &amp; SECURITY" />

                <settings:SettingCard
                    Title="Confirm before opening external links"
                    Margin="0,0,0,32"
                    Description="Ask for confirmation before opening links outside Draft.">
                    <CheckBox IsChecked="{Binding Settings.ConfirmBeforeOpeningExternalLinks, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource SettingsCheckBox}" />
                </settings:SettingCard>

            </StackPanel>
        </ScrollViewer>
    </Border>
</UserControl>
