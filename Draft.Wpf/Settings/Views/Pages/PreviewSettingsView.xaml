<UserControl
    x:Class="Draft.Settings.Views.Pages.PreviewSettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:i18n="clr-namespace:Draft.Localization"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:section="clr-namespace:Draft.Settings.Controls"
    xmlns:settings="clr-namespace:Draft.Settings.Controls"
    d:DesignHeight="2000"
    d:DesignWidth="540"
    mc:Ignorable="d">
    <Border Background="Transparent">
        <ScrollViewer
            HorizontalScrollBarVisibility="Disabled"
            Style="{StaticResource SettingsScrollViewer}"
            VerticalScrollBarVisibility="Auto">
            <StackPanel VerticalAlignment="Top">
                <section:SectionHeader Title="{i18n:Loc settings.preview.rendering}" />

                <settings:SettingFieldCard Title="{i18n:Loc settings.preview.markdownTheme}" Margin="0,0,0,32">
                    <ComboBox
                        Height="36"
                        HorizontalAlignment="Stretch"
                        ItemTemplate="{StaticResource LocalizedComboBoxItemTemplate}"
                        ItemsSource="{Binding MarkdownThemeOptions}"
                        SelectedItem="{Binding Settings.MarkdownTheme, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                        Style="{StaticResource SettingsComboBox}" />
                </settings:SettingFieldCard>

                <section:SectionHeader Title="{i18n:Loc settings.preview.linksSecurity}" />

                <settings:SettingCard
                    Title="{i18n:Loc settings.preview.confirmBeforeOpeningExternalLinks}"
                    Margin="0,0,0,32"
                    Description="{i18n:Loc settings.preview.confirmBeforeOpeningExternalLinks.description}">
                    <CheckBox IsChecked="{Binding Settings.ConfirmBeforeOpeningExternalLinks, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource SettingsCheckBox}" />
                </settings:SettingCard>

            </StackPanel>
        </ScrollViewer>
    </Border>
</UserControl>
