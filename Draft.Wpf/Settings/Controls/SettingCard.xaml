<UserControl
    x:Class="Draft.Settings.Controls.SettingCard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Draft.Settings.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="88"
    d:DesignWidth="500"
    mc:Ignorable="d">
    <Border
        Padding="0,16"
        HorizontalAlignment="Stretch"
        BorderThickness="1">
        <Grid HorizontalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel
                Grid.Column="0"
                Margin="0,0,24,0"
                VerticalAlignment="Center">
                <TextBlock
                    FontFamily="{StaticResource Font.Inter}"
                    FontSize="16"
                    FontWeight="Regular"
                    Foreground="{StaticResource Brush.Text.Primary}"
                    Text="{Binding Title, RelativeSource={RelativeSource AncestorType={x:Type local:SettingCard}}, FallbackValue='Header'}"
                    TextWrapping="Wrap" />
                <TextBlock
                    Margin="0,8,0,0"
                    FontFamily="{StaticResource Font.Inter}"
                    FontSize="11"
                    Foreground="{StaticResource Brush.Text.Secondary}"
                    Text="{Binding Description, RelativeSource={RelativeSource AncestorType={x:Type local:SettingCard}}, FallbackValue='Description Text'}"
                    TextWrapping="Wrap" />
            </StackPanel>

            <ContentPresenter
                Grid.Column="1"
                HorizontalAlignment="Right"
                VerticalAlignment="Center"
                Content="{Binding CardContent, RelativeSource={RelativeSource AncestorType={x:Type local:SettingCard}}}" />
        </Grid>
    </Border>
</UserControl>
