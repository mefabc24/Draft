<UserControl
    x:Class="Draft.Settings.Controls.MenuCustomizationSettingCard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:i18n="clr-namespace:Draft.Localization"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="60"
    d:DesignWidth="500"
    mc:Ignorable="d">
    <Grid x:Name="CardRoot" HorizontalAlignment="Stretch">
        <Grid.RenderTransform>
            <TranslateTransform x:Name="ReorderTransform" />
        </Grid.RenderTransform>

        <Border
            x:Name="CardBorder"
            Padding="0,12"
            HorizontalAlignment="Stretch"
            Background="Transparent"
            BorderThickness="1">
            <Grid HorizontalAlignment="Stretch">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <Border
                    x:Name="DragHandle"
                    Grid.Column="0"
                    Width="36"
                    Height="36"
                    Margin="0,0,12,0"
                    AutomationProperties.Name="{i18n:Loc settings.menuCustomization.dragHandle, Fallback='Drag to reorder'}"
                    Background="Transparent"
                    Cursor="SizeAll"
                    Focusable="False"
                    PreviewMouseLeftButtonDown="DragHandle_PreviewMouseLeftButtonDown"
                    PreviewMouseLeftButtonUp="DragHandle_PreviewMouseLeftButtonUp"
                    PreviewMouseMove="DragHandle_PreviewMouseMove"
                    ToolTip="{i18n:Loc settings.menuCustomization.dragHandle, Fallback='Drag to reorder'}">
                    <Path
                        Width="18"
                        Height="18"
                        Data="{StaticResource Icon.Drag}"
                        Stretch="Uniform">
                        <Path.Style>
                            <Style TargetType="Path">
                                <Setter Property="Fill" Value="{StaticResource Brush.Icon.Default}" />
                                <Style.Triggers>
                                    <DataTrigger
                                        Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Border}}"
                                        Value="True">
                                        <Setter Property="Fill" Value="{StaticResource Brush.Icon.SubtleHover}" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Path.Style>
                    </Path>
                </Border>

                <TextBlock
                    Grid.Column="1"
                    Margin="0,0,24,0"
                    VerticalAlignment="Center"
                    FontFamily="{StaticResource Font.Inter}"
                    FontSize="16"
                    FontWeight="Regular"
                    Foreground="{StaticResource Brush.Text.Primary}"
                    Text="{Binding Title}"
                    TextWrapping="Wrap" />

                <ComboBox
                    Grid.Column="2"
                    Width="200"
                    Height="36"
                    ItemTemplate="{StaticResource LocalizedComboBoxItemTemplate}"
                    ItemsSource="{Binding PlacementOptions}"
                    SelectedItem="{Binding Placement, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                    Style="{StaticResource SettingsComboBox}" />
            </Grid>
        </Border>

        <Rectangle
            x:Name="DropPlaceholder"
            Margin="1"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            IsHitTestVisible="False"
            Opacity="0"
            RadiusX="6"
            RadiusY="6"
            Stroke="{StaticResource Brush.Accent.Primary}"
            StrokeDashArray="5 3"
            StrokeDashCap="Round"
            StrokeThickness="1.5"
            Visibility="Collapsed" />
    </Grid>
</UserControl>
