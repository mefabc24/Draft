<UserControl
    x:Class="Draft.Settings.Controls.ShortcutRecorder"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    x:Name="Root"
    d:DesignHeight="36"
    d:DesignWidth="220"
    Style="{StaticResource ShortcutRecorderControl}"
    mc:Ignorable="d">
    <Border x:Name="MainBorder" Style="{StaticResource ShortcutRecorderBorder}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock
                Grid.Column="0"
                Style="{StaticResource ShortcutRecorderText}"
                Text="{Binding DisplayText, ElementName=Root, FallbackValue='CTRL + S'}" />

            <Button
                x:Name="RecordButton"
                Grid.Column="1"
                AutomationProperties.Name="Record shortcut"
                Click="RecordButton_Click"
                Style="{StaticResource ShortcutRecordButton}">
                <Path
                    Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type Button}}}"
                    Style="{StaticResource ShortcutRecorderIcon}" />
            </Button>
        </Grid>
    </Border>
</UserControl>
