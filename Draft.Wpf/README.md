# Draft.Wpf

Draft.Wpf is the native Windows desktop host for Draft. It is built with WPF and hosts the web workspace from `Draft.Web` through WebView2.

This project owns the desktop shell, native windowing, app startup, local integration, packaging hooks, and Windows-specific behavior.

## Notes

Application icons are based on assets from [Icons8](https://icons8.com/).
